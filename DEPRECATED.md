# Deprecated

이 리포는 [gemma-translate](https://github.com/dalsoop/gemma-translate) 의 `dashboard/` 워크스페이스로 통합되었습니다.

```bash
git clone https://github.com/dalsoop/gemma-translate
cd gemma-translate
cargo run --release -p translate-dashboard -- dashboard/config.json
```
